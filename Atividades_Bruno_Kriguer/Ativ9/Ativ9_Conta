  function Retangulo(x, y) {
            this.base = x;
            this.altura = y;
     
                return this.base * this.altura;
               
            }

var x = parseInt(prompt("Digite o n1"));
var y = parseInt(prompt("Digite o n2"));

alert("A area do triangulo eh " + Retangulo(x,y));
